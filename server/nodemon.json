{
  "watch": ["*.ts", "controller/*.ts", "routes/*.ts", "utils/*.ts", "types/*.ts"],
  "ext": "ts",
  "ignore": ["node_modules", "dist"],
  "exec": "ts-node server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}